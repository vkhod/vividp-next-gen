apiVersion: v1
kind: Service
metadata:
  name: ingestion
  namespace: vividp
spec:
  # ClusterIP (default): reachable only inside the cluster.
  # The ingestion service receives work from NATS, not direct HTTP from outside.
  selector:
    app: ingestion
  ports:
    - port: 8080
      targetPort: 8080
