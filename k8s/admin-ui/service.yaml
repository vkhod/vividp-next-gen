apiVersion: v1
kind: Service
metadata:
  name: admin-ui
  namespace: vividp
spec:
  type: NodePort
  selector:
    app: admin-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080    # Admin UI → http://localhost:30080
